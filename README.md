# ExoColumn

[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.22151365.svg)](https://doi.org/10.5281/zenodo.22151365)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

A one-dimensional radiative–convective equilibrium (RCE) model for planetary
atmospheres, built directly on the [ExoRT](https://github.com/storyofthewolf/ExoRT)
correlated-k radiative transfer code (the radiation core of ExoCAM).

ExoColumn time-marches a single atmospheric column to radiative–convective
equilibrium by calling ExoRT's `aerad_driver` each step, with a prognostic
surface slab, surface turbulent fluxes, moist convection, condensation, and a
stratospheric cold-trap closure. It is intended for habitable-zone and
terrestrial-atmosphere studies where the full 3-D GCM is unnecessary.

## Status

- **Earth validation**: surface temperature 287.8 K, agreeing with the
  independent RCE codes [konrad](https://github.com/atmtools/konrad) (288.0 K)
  and CLIMA/Kasting (289.5 K); energy and water budgets closed. See
  `reference/earth/`.
- **Habitable zone**: inner- and outer-edge calculations reproducing
  Kopparapu et al. (2013), including the moist/runaway greenhouse inner edge,
  the maximum-greenhouse outer edge, and the multi-stellar (F–G–K–M) boundaries.
  See `reference/moist_runaway/`, `reference/max_greenhouse/`,
  `reference/habitablezone/`.

## Requirements

- A copy of **ExoRT** (default location `/models/ExoRT`; override with
  `EXORT_ROOT`). ExoRT is treated as read-only.
- **NetCDF-Fortran** built with the same compiler as ExoColumn.
- A Fortran compiler: **Intel `ifx`** (OneAPI; the default, taken from `PATH`)
  or **`gfortran`**.
- **Python 3** with `numpy`, `matplotlib` and `netCDF4` for the plotting and
  analysis scripts (`pip install -r requirements.txt`). The model itself has no
  Python dependency.

> The system NetCDF on most Linux distributions is built with gfortran and is
> incompatible with `ifx`. For an `ifx` build you need a NetCDF-Fortran compiled
> with `ifx` (see the build notes in `CLAUDE.md`). On Apple Silicon/macOS the
> system NetCDF works directly with `gfortran`.

## Build

Copy `config.mk.example` to `config.mk` and set your local paths
(`NETCDF_ROOT`, `EXORT_ROOT`, optionally `USER_FC`/`PVER`). Then:

```bash
source /opt/intel/oneapi/setvars.sh      # Intel OneAPI (ifx build)
make -C build                            # → run/exocol.exe
make -C build clean
```

The number of vertical layers is a compile-time choice owned by ExoColumn
(not ExoRT). The default is 70; override on the command line:

```bash
make -C build PVER=200
```

A `make clean` is required after changing `PVER`. The build never modifies
ExoRT: it generates local copies of the handful of ExoRT files whose
compile-time settings ExoColumn controls.

### Supplementary stellar spectra

Three BT-Settl spectral energy distributions used by the multi-stellar
habitable-zone figures (host stars at 2000, 2200 and 2400 K) are not yet part of
the public ExoRT distribution, so they ship here instead. Copy them into your
ExoRT tree once, before running those sweeps:

```bash
cp data/exort_extra/*.nc "$EXORT_ROOT"/data/solar/     # default: /models/ExoRT
```

This adds files to ExoRT and modifies none. See `data/exort_extra/README.md`.
All other host-star spectra used here are already distributed with ExoRT.

## Run

Run from the project root so ExoRT can resolve its data-file paths:

```bash
./run/exocol.exe
```

The run is configured by `exocol_config.nml` (three optional namelist blocks:
`&exocol_nml` runtime physics, `&exocol_init` initial conditions, and
`&exocol_composition` dry-air composition). With an empty `input_file` (the
default) the column is built from a cold start; otherwise an ExoRT-format
NetCDF profile is read. Calibrated example configurations are in `presets/`.

Output is written to `iofiles/exocol_out.nc` in ExoRT's `RTprofile_out.nc`
format.

## Plot

```bash
python tools/plot_exocol.py              # 4-panel summary of iofiles/exocol_out.nc
```

Each `reference/<case>/` directory is self-contained and includes its own
plotting script that regenerates the published comparison figure against the
reference data (konrad, CLIMA, Kopparapu et al. 2013).

## Reproducing the published figures

Every figure in the accompanying paper is regenerated by a script in
`reference/`. Each directory caches its sweep results in a `.npz` file, so the
figures re-plot in seconds without re-running the model; delete the cache (or
see the script's header for its environment variables) to recompute from
scratch.

| Figure | Script | Output |
|--------|--------|--------|
| 1 — Present-day Earth vs. konrad and Clima | `reference/earth/plot_earth.py` | `reference/earth/earth_rce.pdf` |
| 2 — Inner-edge line-by-line OLR benchmark | `tools/plot_lbl_olr_figs.py` | `reference/moist_runaway/lbl_olr_benchmark_ihz.pdf` |
| 3 — Outer-edge line-by-line OLR benchmark | `tools/plot_lbl_olr_figs.py` | `reference/max_greenhouse/lbl_olr_benchmark_ohz.pdf` |
| 4 — Inner edge: moist and runaway greenhouse | `reference/moist_runaway/hz_inner.py` | `reference/moist_runaway/hz_inner_nonideal.pdf` |
| 5 — Outer edge: maximum greenhouse | `reference/max_greenhouse/hz_outer.py` | `reference/max_greenhouse/hz_outer.pdf` |
| 6 — Multi-stellar limits vs. Ts and pCO2 | `reference/habitablezone/hz_figure6.py` | `reference/habitablezone/hz_figure6.pdf` |
| 7 — Habitable zone vs. stellar type and mass | `reference/habitablezone/hz_figure7.py` | `reference/habitablezone/hz_figure7.pdf` |
| 8 — Habitable zone vs. planetary mass | `reference/planet_mass/hz_mass.py` | `reference/planet_mass/hz_mass.pdf` |

The habitable-zone boundary coefficients tabulated in the paper are written by
`reference/habitablezone/hz_figure7.py` to
`reference/habitablezone/hz_coefficients.{txt,tex}`.

Figures 6–8 need a binary built at `PVER=200`, and Figures 6 and 7 additionally
need the supplementary stellar spectra copied into ExoRT (see above). Files
carrying an `_a032` suffix are the surface-albedo sensitivity variants described
in `reference/albedo_sensitivity/`.

Radiation results depend on the ExoRT version. This release was run against
ExoRT at commit `4ba8e44`.

## Repository layout

| Path | Contents |
|------|----------|
| `src/` | ExoColumn Fortran source |
| `build/` | Makefile and build-time ExoRT patch tooling |
| `tools/` | Plotting, input-generation, and line-by-line benchmark scripts |
| `test/` | Unit tests for the IAPWS-95, steam-adiabat, and CO2 modules |
| `presets/` | Calibrated example namelists |
| `reference/` | Self-contained validation/HZ cases with data and figures |
| `data/exort_extra/` | Stellar spectra needed by the figures but not yet in ExoRT |
| `CLAUDE.md` | Detailed architecture and developer notes |

## Citation

If you use ExoColumn, please cite both the software and the paper.

**Software** — Haqq-Misra, J. (2026), *ExoColumn: a one-dimensional
radiative-convective equilibrium model for planetary atmospheres*, Zenodo,
[10.5281/zenodo.22151365](https://doi.org/10.5281/zenodo.22151365).

That is the concept DOI, which always resolves to the latest release. To record
exactly which version you ran, cite the version DOI instead — v1.0.0 is
[10.5281/zenodo.22151366](https://doi.org/10.5281/zenodo.22151366). Machine-readable metadata is in
`CITATION.cff`, which GitHub renders as "Cite this repository".

**Paper** — Haqq-Misra, J., Wolf, E. T., & Kopparapu, R. K. (2026), *Validation
of Habitable Zone Limits with a New Radiative-Convective Equilibrium Climate
Model*, submitted to The Astrophysical Journal.

Please also cite [ExoRT](https://github.com/storyofthewolf/ExoRT), which
supplies the radiative transfer: Wolf, E. T., et al. (2022), PSJ, 3, 7.

## License

MIT License © 2026 Blue Marble Space. See `LICENSE`.

ExoRT is distributed separately under its own license and is not included here.
